提取多个文件的文本，包含部分文件类型的目录
对于word，pdf，要考虑文本，目录，同时要注意word的版本差异
对于execl，ppt，要考虑版本差异，同时注意sheet以及每个表的行列的返回模型
对于.json,.csv文件，要抽象返回(封装)
对于email文件，也要抽象返回 利用java-email

利用设计模式，面向对象思想，保证代码的健壮性和可维护性。
最后需要暴露一个服务接口就行了